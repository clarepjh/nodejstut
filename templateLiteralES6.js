// Template Literal (ES6 ONLY) Tutorail

var name = 'John Doe';
var greet = 'Hello ' + name;

var greet2 = `Hello ${ name }` // Adding ` makes it a template string literal
// Template string literal allows us to add parameters into the string without concates (+)

console.log(greet);
console.log(greet2);
